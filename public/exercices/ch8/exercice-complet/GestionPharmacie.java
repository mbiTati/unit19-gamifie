/**
 * EXERCICE COMPLET — Ch.8 : LinkedList + HashMap (P4a)
 * Gestion d'une pharmacie — stock de médicaments
 * SCORING : 100 pts | DURÉE : 40 min
 *
 * Contexte : Une pharmacie gère son stock. Chaque médicament a :
 * code (unique), nom, prix, quantité
 *
 * TODO 1 : Classe Medicament (4 attributs, constructeur, getters, toString)
 * TODO 2 : Classe GestionStock avec LinkedList<Medicament>
 *   - ajouter(Medicament) → boolean (pas de doublon de code)
 *   - rechercherParCode(String) → Medicament
 *   - mettreAJourQuantite(String code, int nouvelleQte) → boolean
 *   - supprimer(String code) → boolean
 *   - listerTous() → void
 *   - rechercherParNom(String) → LinkedList<Medicament> (recherche partielle)
 * TODO 3 : Tests dans main avec scoring intégré
 */
public class GestionPharmacie {
    // TODO : Implémentez Medicament et GestionStock
    // Voir correction pour la solution complète
}