/**
 * EXERCICE COMPLET — Ch.6 : Encapsulation (M3)
 * Système bancaire avec encapsulation complète
 * SCORING : 100 pts | DURÉE : 40 min
 *
 * TODO 1 : Classe CompteBancaire (private: numero, titulaire, solde)
 *   - Constructeur, getters
 *   - deposer(montant) avec validation (> 0)
 *   - retirer(montant) avec validation (> 0, <= solde)
 *   - toString()
 *
 * TODO 2 : Classe Banque (private: HashMap<String, CompteBancaire>)
 *   - creerCompte(numero, titulaire, soldeInitial) — pas de doublon
 *   - getCompte(numero)
 *   - virement(numSource, numDest, montant) — retirer de source, deposer dans dest
 *   - listerComptes()
 *
 * TODO 3 : Montrer que changer HashMap → TreeMap ne casse rien
 */
public class CompteEnBanque {
    // Implémentez les classes ci-dessus
    // Voir correction pour la solution
}